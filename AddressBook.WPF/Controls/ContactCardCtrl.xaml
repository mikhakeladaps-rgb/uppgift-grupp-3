<UserControl x:Class="AddressBook.WPF.Controls.ContactCardCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AddressBook.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="8" Margin="4" Background="#f8f8f8" CornerRadius="8">
        <StackPanel>
            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
            <TextBlock Text="{Binding PhoneNumber}" Foreground="Gray" />
            <TextBlock Text="{Binding Email}" Foreground="Gray" />
        </StackPanel>
    </Border>
</UserControl>
